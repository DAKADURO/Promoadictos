// Single source of truth for the storefront's base categories, shared
// between HomeClient.js (dynamic category list) and CategoryFilter.js
// (default categories) so they can never drift apart.
export const BASE_CATEGORIES = ["Tecnología", "Hogar", "Moda", "Gaming", "Audio", "Deportes", "Belleza"];
